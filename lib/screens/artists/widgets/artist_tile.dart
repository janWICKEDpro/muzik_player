import 'package:flutter/material.dart';

class ArtistTile extends StatelessWidget {
  const ArtistTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
